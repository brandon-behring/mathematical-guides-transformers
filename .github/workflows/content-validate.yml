name: content-validate

on:
  push:
    branches: [main]
    paths: ['src/content/**', 'src/content.config.ts', 'bibliography.bib', 'package.json', '.github/workflows/content-validate.yml']
  pull_request:
    paths: ['src/content/**', 'src/content.config.ts', 'bibliography.bib', 'package.json', '.github/workflows/content-validate.yml']

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm ci
      - run: npm run validate
